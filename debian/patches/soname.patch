From: Emmanuel Arias <eamanu@yaerobi.com>
Date: Fri, 17 Jan 2020 10:57:11 -0300
Subject: Tell meson to add a version to the SONAME

Update Andrej Shadura patch.
---
 src/libcamera/meson.build | 1 +
 1 file changed, 1 insertion(+)

--- libcamera.orig/src/libcamera/meson.build
+++ libcamera/src/libcamera/meson.build
@@ -138,6 +138,7 @@
                            link_with : libcamera_link_with,
                            include_directories : includes,
                            build_rpath : '/',
+                           version: '0.1.0',
                            dependencies : libcamera_deps)
 
 libcamera_dep = declare_dependency(sources : [libcamera_api, libcamera_ipa_api],
